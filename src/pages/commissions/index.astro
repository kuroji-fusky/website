---
import Layout from "$layouts/Layout.astro"
import { routeSections } from "$lib/constants"

const {
  headings: { commissions }
} = routeSections
---

<Layout title={commissions.title} description={commissions.description}>
  commissions index
</Layout>
